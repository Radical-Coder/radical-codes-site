{
  "name": "radical-codes-site",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "vite build",
    "check": "npm run build && npm audit --audit-level=high",
    "dev": "vite --host 0.0.0.0"
  },
  "devDependencies": {
    "vite": "^8.0.16"
  }
}
